[package]
name = "pokie-desktop"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
app-api = { path = "../../crates/app-api" }
tauri = { version = "2.10.3" }

[build-dependencies]
tauri-build = { version = "2.5.6" }

[workspace]
