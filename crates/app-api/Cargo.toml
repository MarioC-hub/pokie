[package]
name = "app-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
config-core = { path = "../config-core" }
serde = { version = "1.0", features = ["derive"] }
solver-core = { path = "../solver-core" }
