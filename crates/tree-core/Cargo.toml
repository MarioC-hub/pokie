[package]
name = "tree-core"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
config-core = { path = "../config-core" }
poker-core = { path = "../poker-core" }
range-core = { path = "../range-core" }
